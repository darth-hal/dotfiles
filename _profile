# ~/.profile: executed by the command interpreter for login shells.
# This file is not read by bash(1), if ~/.bash_profile or ~/.bash_login
# exists.
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.

# the default umask is set in /etc/profile; for setting the umask
# for ssh logins, install and configure the libpam-umask package.
#umask 022

# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
	. "$HOME/.bashrc"
    fi
fi

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi
export LANGUAGE="ja:en"
export LC_MESSAGES="ja_JP.UTF-8"
export LC_CTYPE="ja_JP.UTF-8"
export LC_COLLATE="ja_JP.UTF-8"


# for Android
if [ -d "$HOME/local/android-sdk-linux" ]; then
    export ANDROID_HOME="$HOME/local/android-sdk-linux"
    PATH="$ANDROID_HOME/tools:$ANDROID_HOME/platform-tools:$PATH"
fi
# for ANT
if [ -d "$HOME/local/apache-ant-1.8.2" ]; then
    export ANT_HOME="$HOME/local/apache-ant-1.8.2"
    PATH="$ANT_HOME/bin:$PATH"
fi

# for MAVEN
if [ -d "$HOME/local/apache-maven-3.0.4" ]; then
    export MAVEN_HOME="$HOME/local/apache-maven-3.0.4"
    PATH="$MAVEN_HOME/bin:$PATH"
fi
# for sun java
if [ -d "$HOME/local/jdk1.7.0_02" ]; then
    export JAVA_HOME="$HOME/local/jdk1.7.0_02"
    PATH="$JAVA_HOME/bin:$PATH"
    WEBIDE_JDK="$HOME/local/jdk1.7.0_02"
fi
#for JBoss 7.0.2
if [ -d "$HOME/local/jboss-as-7.0.2.Final" ]; then
    export JBOSS_HOME="$HOME/local/jboss-as-7.0.2.Final"
fi

# for postgresql
if [ -d "$HOME/local/pgsql" ]; then
    LD_LIBRARY_PATH="/usr/local/pgsql/lib"
    export LD_LIBRARY_PATH

    PATH="$HOME/local/pgsql/bin:$PATH"
    export PATH

    MANPATH="$HOME/local/pgsql/man:$MANPATH"
    export MANPATH
fi

